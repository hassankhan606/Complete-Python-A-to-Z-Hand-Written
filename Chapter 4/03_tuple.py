# Tuples in Python 
# Definition: A tuple is a collection of items that are ordered and unchangeable. It allows duplicate members.
# Tuples are similar to lists, but they are immutable, meaning once created, their content cannot be changed.

a= (1,"Hassan",2,"Ali",3,"Ahmed")
print(a)
print(type(a))